---
import Layout from '../layouts/Layout.astro';

interface Props {
  title: string,
  description: string
}
---

<Layout {...Astro.props}>
	<div class='hero h-screen'>
	  <div class='w-full h-full bg-[url("/assets/background.webp")] bg-cover bg-fixed -z-2'></div>
	  <div class='w-full h-full bg-black bg-cover bg-fixed -z-1 opacity-65'></div>

	  <div class='hero-content w-screen flex-col py-8 lg:px-40'>
	    <slot /> 
    </div>
  </div> 
</Layout>
